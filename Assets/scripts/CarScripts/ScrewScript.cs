using UnityEngine;

public class ScrewScript : MonoBehaviour
{
    
    //public GameObject holdingPart;
  
    //public Vector3 holdingPartPosition;
    //public Quaternion holdingPartRotation;
    //// Start is called once before the first execution of Update after the MonoBehaviour is created
    //void Start()
    //{
        
    //}
    //public void AttachPart(GameObject obj)
    //{
    //    holdingPartPosition = obj.transform.position - gameObject.transform.position;
    //    holdingPartRotation = gameObject.transform.rotation;
        
    //    holdingPart = obj;
    //}
    //// Update is called once per frame
    //void Update()
    //{
    //    if (holdingPart != null)
    //    {
    //        holdingPart.transform.position = gameObject.transform.position + holdingPartPosition;
    //        holdingPart.transform.rotation = gameObject.transform.rotation;
    //    }
    //}
}
